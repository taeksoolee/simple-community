<%# 재사용 가능한 플래시 메시지
  locals: type (:notice | :alert), message, auto_delay (ms, nil이면 type에 따라 기본값)
-%>
<% auto_delay ||= (type == :notice ? 5000 : 0) %>
<div class="w-full"
     data-controller="dismiss"
     data-dismiss-auto-delay-value="<%= auto_delay %>">
  <div class="flex items-start gap-3 rounded-lg border <%= type == :alert ? 'border-red-200 dark:border-red-800' : 'border-green-200 dark:border-green-800' %> bg-white/95 dark:bg-gray-800/95 backdrop-blur px-4 py-3 shadow-lg shadow-gray-200/50">
    <p class="flex-1 text-sm font-medium <%= type == :alert ? 'text-red-800 dark:text-red-200' : 'text-green-800 dark:text-green-200' %>"><%= message %></p>
    <button type="button"
            data-action="click->dismiss#close"
            class="flex-shrink-0 p-1 rounded-md <%= type == :alert ? 'text-red-500 dark:text-red-400 hover:bg-red-50 dark:hover:bg-red-900/30' : 'text-green-600 dark:text-green-400 hover:bg-green-50 dark:hover:bg-green-900/30' %> transition-colors"
            aria-label="닫기">
      <%= heroicon "x-mark", variant: :outline, options: { class: "w-4 h-4" } %>
    </button>
  </div>
</div>
