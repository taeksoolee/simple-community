<div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
  <div class="bg-white shadow overflow-hidden sm:rounded-lg">
    <div class="px-4 py-5 sm:px-6 flex justify-between items-start">
      <div>
        <h1 class="text-3xl font-bold text-gray-900"><%= @post.title %></h1>
        <p class="mt-1 text-sm text-gray-500">
          작성자: <%= @post.user.email_address %> | 
          <%= @post.created_at.strftime('%Y-%m-%d %H:%M') %>
        </p>
      </div>
      <% if Current.user == @post.user %>
        <div class="flex space-x-2">
          <%= link_to "수정", edit_post_path(@post), class: "inline-flex items-center px-3 py-2 border border-gray-300 shadow-sm text-sm leading-4 font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50" %>
          <%= button_to "삭제", post_path(@post), method: :delete, data: { confirm: "정말 삭제하시겠습니까?" }, 
              class: "inline-flex items-center px-3 py-2 border border-transparent text-sm leading-4 font-medium rounded-md text-white bg-red-600 hover:bg-red-700" %>
        </div>
      <% end %>
    </div>
    <div class="border-t border-gray-200 px-4 py-5 sm:px-6">
      <div class="prose max-w-none">
        <%= simple_format(@post.body) %>
      </div>
    </div>
  </div>

  <div class="mt-6">
    <%= link_to "← 목록으로", posts_path, class: "text-indigo-600 hover:text-indigo-500" %>
  </div>

  <div class="mt-8">
    <h2 class="text-2xl font-bold text-gray-900 mb-4">댓글 (<%= @post.comments.where(commentable_type: "Post").count %>)</h2>
    
    <% if Current.user %>
      <div class="mb-6 bg-gray-50 p-6 rounded-lg">
        <%= form_with url: comments_path, method: :post, class: "space-y-4" do |form| %>
          <%= hidden_field_tag "comment[commentable_type]", "Post" %>
          <%= hidden_field_tag "comment[commentable_id]", @post.id %>
          
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-2">댓글 작성</label>
            <%= text_area_tag "comment[body]", nil, rows: 4,
                class: "block w-full px-4 py-3 rounded-lg border-2 border-gray-300 shadow-sm placeholder-gray-400 focus:outline-none focus:border-indigo-500 focus:ring-2 focus:ring-indigo-200 transition duration-200 sm:text-sm resize-none",
                placeholder: "댓글을 입력하세요..." %>
          </div>
          
          <div class="flex justify-end">
            <%= submit_tag "댓글 작성", class: "px-5 py-2.5 border border-transparent text-sm font-medium rounded-lg text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500 transition duration-200" %>
          </div>
        <% end %>
      </div>
    <% else %>
      <div class="mb-6 p-6 bg-gradient-to-r from-indigo-50 to-blue-50 rounded-lg border border-indigo-100">
        <p class="text-gray-700 text-center">
          댓글을 작성하려면 
          <%= link_to "로그인", new_session_path, class: "font-semibold text-indigo-600 hover:text-indigo-500 underline" %>
          하세요
        </p>
      </div>
    <% end %>

    <div id="comments">
      <% top_level_comments = @post.comments.where(commentable_type: "Post").includes(:user, :comments).order(created_at: :asc) %>
      <% if top_level_comments.any? %>
        <% top_level_comments.each do |comment| %>
          <%= render partial: 'comments/comment', locals: { comment: comment, depth: 0 } %>
        <% end %>
      <% else %>
        <p class="text-gray-500 text-center py-8">아직 댓글이 없습니다. 첫 댓글을 작성해보세요!</p>
      <% end %>
    </div>
  </div>

  <script>
    function toggleReplyForm(commentId) {
      const form = document.getElementById(`reply-form-${commentId}`);
      form.classList.toggle('hidden');
    }
  </script>
</div>
